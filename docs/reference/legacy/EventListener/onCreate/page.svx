
`onCreate`는 스크립트 액티비티의
[`onCreate`](https://developer.android.com/reference/android/app/Activity#onCreate(android.os.Bundle))가 호출되면 동작하는 이벤트 리스너입니다.
## 구문

~~~js
function onCreate(savedInstanceState, activity) {

}
~~~

### 인자

<params>
<param-item param="savedInstanceState" type="android.os.Bundle">
`activity`의 이전 인스턴스 상태가 담긴 `Bundle`입니다.
</param-item>
<param-item param="activity" type="android.app.Activity">
해당 이벤트가 발생했을 때의 액티비티입니다.
</param-item>
</params>
