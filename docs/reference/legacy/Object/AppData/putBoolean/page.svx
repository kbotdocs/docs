
앱 데이터에 `key: boolean` (`String: Boolean` 형식) 데이터를 씁니다.
## 구문

~~~js
AppData.putBoolean(key, boolean);
~~~

### 매개변수

<params>
<param-item param="key" type="String">
쓸 앱 데이터의 키
</param-item>
<param-item param="boolean" type="Boolean">
`key`에 대한 `Boolean`형 데이터
</param-item>
</params>
