
앱 데이터에 `key: integer` (`String: Number(정수)` 형식) 데이터를 씁니다.
## 구문

~~~js
AppData.putInt(key, integer);
~~~

### 매개변수

<params>
<param-item param="key" type="String">
쓸 앱 데이터의 키
</param-item>
<param-item param="integer" type="Number">
`key`에 대한 `Number`(정수)형 데이터
</param-item>
</params>
