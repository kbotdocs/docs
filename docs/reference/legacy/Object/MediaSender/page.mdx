---
title: MediaSender
category: 레거시 API
compat:
  data: legacy/Object/MediaSender
  feature: "*"
---

<Section>
  `MediaSender` 객체는 텍스트 외의 미디어 파일을 전송할 수 있게 하는 객체입니다. 이 객체는 `MediaSender` 인스턴스를 생성하여 사용합니다.
</Section>
<Section>
  ## 인스턴스 메소드

  <Table>
    <Thead>
      <Tr>
        <Th colSpan="2">`MediaSender` 객체의 인스턴스 메소드</Th>
      </Tr>
    </Thead>
    <Tbody>
      <Tr>
        <Td><span>`void`</span></Td>
        <Td>
          [`clearCache`](/reference/legacy/Object/MediaSender/clearCache)`()` \
          캐시를 삭제합니다.
        </Td>
      </Tr>
      <Tr>
        <Td><span>`String`</span></Td>
        <Td>
          [`getBaseDirectory`](/reference/legacy/Object/MediaSender/getBaseDirectory)`()` \
          전송할 미디어 파일의 기본 디렉토리를 반환합니다.
        </Td>
      </Tr>
      <Tr>
        <Td><span>`String[]`</span></Td>
        <Td>
          [`getCachedUrls`](/reference/legacy/Object/MediaSender/getCachedUrls)`()` \
          캐시에 저장된 모든 URL을 배열 형태로 반환합니다.
        </Td>
      </Tr>
      <Tr>
        <Td><span>`Number`</span></Td>
        <Td>
          [`getCacheSize`](/reference/legacy/Object/MediaSender/getCacheSize)`()` \
          캐시의 크기(Byte)를 반환합니다.
        </Td>
      </Tr>
      <Tr>
        <Td><span>`String[]`</span></Td>
        <Td>
          [`getSupportedFormats`](/reference/legacy/Object/MediaSender/getSupportedFormats)`()` \
          `MediaSender` 객체가 지원하는 미디어 파일의 확장자를 배열 형태로 반환합니다.
        </Td>
      </Tr>
      <Tr>
        <Td><span>`Boolean`</span></Td>
        <Td>
          [`returnToAppNow`](/reference/legacy/Object/MediaSender/returnToAppNow)`()` \
          카카오톡봇 앱으로 돌아가고 정상적인 수행 여부를 반환합니다.
        </Td>
      </Tr>
      <Tr>
        <Td><span>`Boolean`</span></Td>
        <Td>
          [`send`](/reference/legacy/Object/MediaSender/send)`(roomIdentifier: any, mediaResource: any, timeoutMs?: Number = 25000)` \
          특정 방에 미디어 파일을 전송하고 미디어 파일의 전송 성공 여부를 반환합니다.
        </Td>
      </Tr>
    </Tbody>
  </Table>
</Section>