
<section>
  문자열 `str`에서 반전 문자를 삭제한 값을 반환합니다.
</section>
<section>
  ## 구문

  ~~~js
  Utils.removeRLO(str);
  ~~~

  <section>
  ### 매개변수

  <params>
    <param-item param="str" type="String">
    반전 문자를 삭제할 문자열
    </param-item>
  </params>
  </section>
  <section>
  ### 반환값

  `String` - 문자열 `str`에서 반전 문자를 삭제한 값을 반환합니다.
  </section>
</section>