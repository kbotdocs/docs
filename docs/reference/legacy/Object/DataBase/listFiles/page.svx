---
title: DataBase.listFiles()
category: 레거시 API
compat:
  data: legacy/Object/DataBase
  feature: listFiles
---

<Section>
  `DataBase` 객체가 사용하는 디렉토리에 존재하는 파일의 이름을 배열로 반환합니다.
</Section>
<Section>
  ## 구문

  ~~~js
  DataBase.listFiles();
  ~~~

  <Section>
    ### 반환값

    `String[]` - `DataBase` 객체가 사용하는 디렉토리에 존재하는 파일의 이름을 배열로 반환합니다.
  </Section>
</Section>