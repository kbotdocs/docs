
<section>
  `DataBase` 객체가 사용하는 디렉토리의 경로를 반환합니다.
</section>
<section>
  ## 구문

  ~~~js
  DataBase.getDataBaseDir();
  ~~~

  <section>
  ### 반환값

  `String` - `DataBase` 객체가 사용하는 디렉토리의 경로를 반환합니다.
  </section>
</section>