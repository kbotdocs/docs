---
title: App.isMainThread()
category: API2
compat:
  data: api2/Object/App
  feature: isMainThread
---

<Section>
    # App.isMainThread()

    해당 메소드를 실행한 쓰레드가 메인 쓰레드인지의 여부를 반환합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    App.isMainThread();
    ~~~

    <Section>
        ### 반환값

        `Boolean` - 해당 메소드를 실행한 쓰레드가 메인 쓰레드이면 `true`, 아니면 `false`를 반환합니다.
    </Section>
</Section>