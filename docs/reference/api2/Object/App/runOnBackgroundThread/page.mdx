---
title: App.runOnBackgroundThread()
category: API2
compat:
  data: api2/Object/App
  feature: runOnBackgroundThread
---

<Section>
    # App.runOnBackgroundThread()

    백그라운드 쓰레드 상에서 특정 함수를 실행합니다.
</Section>
<Section>
    ## 구문

    ~~~js
    App.runOnBackgroundThread(task);
    App.runOnBackgroundThread(task, onComplete);
    ~~~

    <Section>
        ### 매개변수

        <ParamList>
            <ParamItem param={"task"} type={"() => Any"}>
                백그라운드 쓰레드 상에서 실행할 함수
            </ParamItem>
            <ParamItem param={"onComplete"} type={"(error, result) => Any"} optional>
                결과를 처리할 함수
            </ParamItem>
            <ParamList>
                <ParamItem param={"error"} type={"org.graalvm.polyglot.PolyglotException"}>
                    `task` 함수 실행 도중 일어난 에러의 객체를 전달받는 인자
                </ParamItem>
                <ParamItem param={"result"} type={"Any"}>
                    `task` 함수의 반환값을 전달받는 인자
                </ParamItem>
            </ParamList>
        </ParamList>
    </Section>
</Section>