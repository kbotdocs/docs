
<section>
  모든 `Bot` 객체 인스턴스의 이름을 배열로 반환합니다.
</section>
<section>
  ## 구문

  ~~~js
  BotManager.getBotNames();
  ~~~

  <section>
  ### 반환값

  `String[]` - 모든 `Bot` 객체 인스턴스의 이름을 배열로 반환합니다.
  </section>
</section>