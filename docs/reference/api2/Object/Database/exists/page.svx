
`fileName` 이름의 파일이나 디렉토리가 존재하는지의 여부를 반환합니다.
## 구문

~~~js
Database.exists(fileName);
~~~

### 매개변수

<params>
<param-item param="fileName" type="String">
존재 여부를 확인할 파일 또는 디렉토리의 이름
</param-item>
</params>
### 반환값

`Boolean` - `fileName` 이름의 파일 또는 디렉토리가 존재할 경우 `true`, 존재하지 않을 경우 `false`를 반환합니다.
