
`fileName` 이름의 파일의 내용을 문자열로 반환합니다. 파일이 존재하지 않을 경우 빈 문자열을 반환합니다.
## 구문

~~~js
Database.readString(fileName);
~~~

### 매개변수

<params>
<param-item param="fileName" type="String">
내용을 문자열로 읽을 파일 이름
</param-item>
</params>
### 반환값

`String` - `fileName` 이름의 파일의 내용을 반환합니다. 파일이 존재하지 않을 경우 빈 문자열을 반환합니다.
