
앱 데이터에 `key`로 대응되는 데이터가 존재하는지의 여부를 반환합니다.
## 구문

~~~js
AppData.contains(key);
~~~

### 매개변수

<params>
  <param-item param="key" type="String">
    존재 여부를 확인할 데이터의 키
  </param-item>
</params>
### 반환값

`Boolean` - `key`로 대응되는 데이터가 존재할 경우 `true`, 존재하지 않을 경우 `false`를 반환합니다.
