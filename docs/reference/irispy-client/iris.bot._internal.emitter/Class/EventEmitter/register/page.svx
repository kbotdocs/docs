
특정 이벤트에 대한 핸들러 함수를 등록합니다.
## 구문

~~~py
eventEmitter.register(name, func)
~~~

### 매개변수

<params>
<param-item param="name" type="str">
핸들러를 등록할 이벤트 이름
</param-item>
<param-item param="func" type="Callable">
등록할 핸들러 함수
</param-item>
</params>
