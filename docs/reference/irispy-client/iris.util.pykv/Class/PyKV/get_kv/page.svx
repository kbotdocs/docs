
데이터베이스에서 특정 키에 대응하는 키-값 쌍을 반환합니다. 키에 대응하는 값이 존재하지 않을 경우 `False`를 반환합니다.
## 구문

~~~py
pyKV.get_kv(key)
~~~

### 매개변수

<params>
<param-item param="key" type="str">
가져올 값에 대응하는 키
</param-item>
</params>
### 반환값

`dict | bool` - 특정 키에 대응하는 키-값 쌍을 반환합니다. 키에 대응하는 값이 존재하지 않을 경우 `False`를 반환합니다.
