---
title: Utils.removeTags()
category: 레거시 API
compat:
  - category: legacy/Object/Utils
    feature: removeTags
---

<Section>
  문자열 `html`에서 HTML 태그를 삭제한 값을 반환합니다.
</Section>
<Section>
  ## 구문

  ~~~js
  Utils.removeTags(html: String);
  ~~~

  <Section>
    ### 매개변수

    <ParamList>
      <ParamItem param={"html"} type={"String"}>
        반전 문자를 삭제할 문자열
      </ParamItem>
    </ParamList>
  </Section>
  <Section>
    ### 반환값

    `String` - 문자열 `html`에서 HTML 태그를 삭제한 값을 반환합니다.
  </Section>
</Section>